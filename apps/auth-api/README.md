# auth-api

- [ ] add description to the auth-api

## Install

```bash
pnpm add @bmod/auth-api
pnpm add -g @bmod/auth-api
```

## Run with cli too

```bash
auth-api run --port 3000 --prefix api --profile some-profile
```

## Integrate with your own app

```typescript
import { AuthApiModule } from '@bmod/auth-api';

// ...
```
