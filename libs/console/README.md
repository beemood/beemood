# console

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build console` to build the library.

## Running unit tests

Run `nx test console` to execute the unit tests via [Vitest](https://vitest.dev/).
