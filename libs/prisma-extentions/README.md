# prisma-extentions

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build prisma-extentions` to build the library.

## Running unit tests

Run `nx test prisma-extentions` to execute the unit tests via [Vitest](https://vitest.dev/).
