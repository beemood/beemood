/**
 * Library generator options
 */
export interface LibGeneratorSchema {
  /**
   * Target directory of the generated files. The directory paths must use "/" for path segmentation such as `libs/library-name`, `tools/library-name`
   */
  directory: string;
}
