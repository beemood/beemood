# datetime

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build datetime` to build the library.

## Running unit tests

Run `nx test datetime` to execute the unit tests via [Vitest](https://vitest.dev/).
